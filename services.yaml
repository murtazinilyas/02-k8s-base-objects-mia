apiVersion: v1
kind: Service
metadata:
  name: netology-svc
  labels:
    app: netology-web
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: netology-web